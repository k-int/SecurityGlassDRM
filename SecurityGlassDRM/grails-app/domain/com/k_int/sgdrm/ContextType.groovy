package com.k_int.sgdrm


/**
 * The type of a context - envisaged to either be 'user' or 'organisation' but
 * with scope for extension
 */
class ContextType {

    String              name;
    
    static constraints = {
    }
}
